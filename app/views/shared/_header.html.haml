#header
  .section-item
    #logo!= link_to @site_config.site_title, root_path
  .section-main
    .section-item
      - if can? :create, UserSession
        != render :partial => 'shared/login_bar'
      - elsif can? :destroy, @current_session
        #logout_bar
          != button_to "Logout", user_session_path, :method => :delete
