require 'spec_helper'

describe DocumentationPage do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    DocumentationPage.create!(@valid_attributes)
  end
end
